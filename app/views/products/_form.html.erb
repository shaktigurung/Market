<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_area :description, class:"form-control" %>
  </div>

  <%= form.fields_for :product_image do |t| %>
    <div>
      <%= t.label :image %>
      <%= t.file_field :image, :multiple => true %>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, min: 0, class:"form-control"%>
  </div>

  <div class="form-group">
    <%= form.label :price %>
    <%= form.number_field :price, min: 1, max: 9999 , class:"form-control"%>
  </div>

  <div class="form-group">
    <%= form.label :category %>
    <%= form.select :category, [["Shirt", "Shirt"],["Jumper", "Jumper"], ["Pants", "Pants"], ['Underwear', 'Underwear'], ["Collectable", "Collectable"], ["Gadget", "Gadget"]], class:"form-control" %>
  </div>
  
  <div class="form-group">
    <%= form.label :condition %>
    <%= form.select :condition, [["Unused", "Unused"],["Used", "Used"]], class:"form-control"  %>
  </div>

  <div class="actions">
    <%= form.submit "Create Product", class:"submit" %>
  </div>
<% end %>
